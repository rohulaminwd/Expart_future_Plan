import { colors } from "./colors";
const pins = {
  startPins: 3,
  pinSize: 2,
  pinGap: 20,
};

const ball = {
  ballSize: 5.7,
};

const engine = {
  engineGravity: 1.0,
};

const world = {
  width: 350,
  height: 390,
};

export const config = {
  pins,
  ball,
  engine,
  world,
  colors,
};
